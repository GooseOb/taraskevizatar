{
	"name": "taraskevizatar",
	"author": "GooseOb",
	"license": "MIT",
	"type": "module",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/GooseOb/taraskevizatar.git"
	},
	"scripts": {
		"dev": "concurrently --kill-others \"npm run --prefix api start:dev\" \"npm run --prefix client dev:api\"",
		"deploy": "npm run --prefix client deploy",
		"prepare": "husky install"
	},
	"devDependencies": {
		"concurrently": "^8.0.1",
		"husky": "^8.0.3",
		"prettier": "3.0.0"
	},
	"dependencies": {
		"taraskevizer": "^1.1.1"
	}
}
