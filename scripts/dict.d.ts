type dict = {
    [key: string]: RegExp;
};
declare const latinLettersUpCase: dict;
declare const gobj: ({
    [key: string]: string;
});
declare const wordlist: dict, softers: dict, arabLetters: dict, latinLetters: dict;
export { wordlist, softers, arabLetters, latinLetters, latinLettersUpCase, gobj };
